import 'package:flutter/material.dart';

/// User approval page — approve or reject pending clan member registrations.
class UserApprovalPage extends StatelessWidget {
  const UserApprovalPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement in Prompt 2
    return const Scaffold(
      body: Center(child: Text('User Approval')),
    );
  }
}
