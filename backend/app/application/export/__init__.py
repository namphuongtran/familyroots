"""Clan export bounded context — "bản sao ngàn đời" (thousand-generation copy).

A lossless archive of everything a clan needs to survive outside the SaaS:
persons (incl. soft-deleted), memberships, branches, marriages, parent-child
edges (incl. soft-deleted), events, and a live-documents manifest with
presigned download URLs.
"""
