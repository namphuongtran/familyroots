"""Supabase Storage upload/delete service."""

# TODO: implement in Prompt 2
#
# This service will provide:
# - upload_file(bucket: str, path: str, file: UploadFile) -> str — returns public URL
# - delete_file(bucket: str, path: str) -> bool
# - get_signed_url(bucket: str, path: str, expires_in: int) -> str
