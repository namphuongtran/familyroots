"""FCM push notification sender service."""

# TODO: implement in Prompt 2
#
# This service will provide:
# - send_notification(user_id: UUID, title: str, body: str) -> bool
# - send_bulk_notifications(user_ids: list[UUID], title: str, body: str) -> dict
# - send_anniversary_reminders() — called by APScheduler daily
