"""Recursive family tree construction service."""

# TODO: implement in Prompt 2
#
# This service will provide:
# - build_tree(root_member_id: UUID) -> TreeNode — recursive tree construction
# - get_ancestors(member_id: UUID, depth: int) -> list[TreeNode]
# - get_descendants(member_id: UUID, depth: int) -> list[TreeNode]
