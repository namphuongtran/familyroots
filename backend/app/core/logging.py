"""Structured JSON logging configuration using structlog."""

# TODO: implement in Prompt 2
#
# This module will provide:
# - configure_logging() — set up structlog with JSON output
# - get_logger(name: str) — return a bound logger instance
# - Request ID injection via middleware
# - Correlation with Sentry breadcrumbs
