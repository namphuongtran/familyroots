Please review all my uncommitted changes using `git status` and `git diff`.
Then, generate a concise Conventional Commits style message summarizing the changes.
Show me the proposed commit message.
If I approve, run the following commands to sync my work:
1. `git add -A`
2. `git commit -m "<approved_message>"`
3. `git push origin HEAD`