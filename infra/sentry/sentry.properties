# Sentry configuration
# See: https://docs.sentry.io/platforms/python/configuration/

defaults.org=familyroots
defaults.project=familyroots-api

# Auth token is set via SENTRY_AUTH_TOKEN environment variable
# DSN is set via SENTRY_DSN environment variable
