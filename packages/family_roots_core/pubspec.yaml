name: family_roots_core
description: Shared pure Dart package — domain entities, API contracts, and utilities used by both mobile and web apps.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.5

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
